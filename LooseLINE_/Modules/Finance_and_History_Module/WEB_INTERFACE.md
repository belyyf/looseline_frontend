# 🌐 Веб-интерфейс LOOSELINE Wallet

## ✅ Что создано

Полноценный веб-интерфейс для кошелька, который работает прямо на сервере!

### Структура:
```
backend/
├── templates/
│   └── index.html          # Главная страница с дизайном
├── static/
│   ├── css/
│   │   └── style.css       # Стили интерфейса
│   └── js/
│       └── app.js          # JavaScript для работы с API
└── main.py                 # Настроен для обслуживания статики
```

## 🚀 Как использовать

### 1. Установите зависимости:
```bash
cd backend
pip install -r requirements.txt
```

### 2. Запустите сервер:
```bash
python main.py
```

### 3. Откройте в браузере:
```
http://localhost:8000/
```

## 🎨 Функции интерфейса

### ✅ Реализовано:
- **Просмотр баланса** с метриками (ROI, Win Rate, Net Profit)
- **Пополнение баланса** через Stripe (новые и сохранённые карты)
- **Вывод средств** на банковский счёт
- **История операций** с фильтрацией
- **Экспорт отчётов** в CSV и PDF
- **Управление способами оплаты**
- **Адаптивный дизайн** (работает на мобильных)

### 🎨 Дизайн:
- Современный градиентный дизайн
- Анимации и переходы
- Toast уведомления
- Loading индикаторы
- Адаптивная вёрстка

## 📱 Интерфейс включает:

1. **Header** - Логотип и User ID
2. **Balance Card** - Текущий баланс и статистика
3. **Deposit Form** - Форма пополнения с Stripe Elements
4. **Withdraw Form** - Форма вывода средств
5. **History Section** - История всех операций
6. **Filters** - Фильтрация по типу операций

## 🔧 Настройка Stripe

Для работы платежей нужно:

1. Получить Stripe ключи из [Dashboard](https://dashboard.stripe.com/apikeys)
2. Добавить в `backend/.env`:
   ```env
   STRIPE_PUBLISHABLE_KEY=pk_test_...
   STRIPE_SECRET_KEY=sk_test_...
   ```
3. Перезапустить сервер

## 🎯 API Endpoints

Интерфейс использует те же API endpoints:
- `GET /api/wallet/balance` - Получить баланс
- `POST /api/wallet/deposit` - Пополнить баланс
- `POST /api/wallet/withdraw` - Вывести средства
- `GET /api/wallet/history` - История операций
- `POST /api/wallet/export` - Экспорт отчёта

## 📝 Примечания

- Весь дизайн находится на сервере
- Не требуется отдельный frontend сервер
- Работает из коробки после запуска
- Полностью интегрирован с API

## 🐛 Отладка

Если интерфейс не загружается:

1. Проверьте, что сервер запущен:
   ```bash
   curl http://localhost:8000/health
   ```

2. Проверьте логи сервера в `backend/logs/app.log`

3. Откройте консоль браузера (F12) для просмотра ошибок JavaScript

4. Убедитесь, что все файлы созданы:
   ```bash
   ls backend/templates/
   ls backend/static/css/
   ls backend/static/js/
   ```

---

**Готово!** 🎉 Теперь у вас есть полноценный веб-интерфейс на сервере!

