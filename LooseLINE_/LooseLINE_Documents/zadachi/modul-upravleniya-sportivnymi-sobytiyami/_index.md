---
order: 2
title: Модуль управления спортивными событиями
---



**Входные данные (от пользователей через Frontend):**

-  Запросы на просмотр событий

-  Фильтры (по спорту, лиге, статусу)

-  Админ-запросы (добавить, отредактировать, удалить событие)

**Выходные данные (возвращает на Frontend):**

-  Списки событий с коэффициентами

-  Отфильтрованные события

-  Сообщения об успехе/ошибке

---

## 🗄️ ЭТАП 1: СОЗДАНИЕ ТАБЛИЦ БД (1-2 часа)

### ЧТО ДЕЛАТЬ?

Создать **5 таблиц в PostgreSQL** для хранения данных о спортивных событиях.

### ТАБЛИЦА 1: sports_types (Виды спорта)

```sql
CREATE TABLE sports_types (
    sport_id SERIAL PRIMARY KEY,
    sport_name VARCHAR(50) UNIQUE NOT NULL,
    icon_emoji VARCHAR(10),
    description TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

Данные:
sport_id | sport_name  | icon_emoji | description
1        | football    | ⚽         | Футбол
2        | basketball  | 🏀         | Баскетбол
3        | tennis      | 🎾         | Теннис
4        | hockey      | 🏒         | Хоккей
5        | baseball    | ⚾         | Бейсбол

Зачем:
- Централизованное хранилище видов спорта
- Быстро фильтровать события по спорту
- Эмодзи для красивого отображения
```

### ТАБЛИЦА 2: leagues (Лиги)

```sql
CREATE TABLE leagues (
    league_id SERIAL PRIMARY KEY,
    league_name VARCHAR(100) UNIQUE NOT NULL,
    sport_id INTEGER NOT NULL,
    country VARCHAR(50),
    level VARCHAR(20),
    created_at TIMESTAMP DEFAULT NOW(),
    FOREIGN KEY (sport_id) REFERENCES sports_types(sport_id)
);

Данные:
league_id | league_name    | sport_id | country      | level
1         | Premier League | 1        | England      | top
2         | La Liga        | 1        | Spain        | top
3         | NBA            | 2        | USA          | top
4         | Wimbledon      | 3        | England      | grand_slam
5         | KHL            | 4        | Russia       | top

Зачем:
- Каждому событию можно указать лигу
- Быстро фильтровать по лиге
- Информация о стране и уровне
```

### ТАБЛИЦА 3: events (События/Матчи)

```sql
CREATE TABLE events (
    event_id SERIAL PRIMARY KEY,
    sport_id INTEGER NOT NULL,
    league_id INTEGER NOT NULL,
    home_team VARCHAR(100) NOT NULL,
    away_team VARCHAR(100) NOT NULL,
    event_datetime TIMESTAMP NOT NULL,
    status VARCHAR(20) DEFAULT 'scheduled',
    home_score INTEGER,
    away_score INTEGER,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    FOREIGN KEY (sport_id) REFERENCES sports_types(sport_id),
    FOREIGN KEY (league_id) REFERENCES leagues(league_id)
);

Данные:
event_id | sport_id | league_id | home_team        | away_team  | event_datetime       | status
1        | 1        | 1         | Manchester United| Liverpool  | 2025-12-15 18:00:00 | scheduled
2        | 1        | 2         | Real Madrid      | Barcelona  | 2025-12-15 19:45:00 | live
3        | 1        | 1         | Arsenal          | Chelsea    | 2025-12-16 16:00:00 | finished
4        | 2        | 3         | Lakers           | Celtics    | 2025-12-15 20:00:00 | scheduled
5        | 3        | 4         | Djokovic         | Sinner     | 2025-12-15 15:30:00 | live

Столбцы:
- status: "scheduled" (будущее), "live" (идёт сейчас), "finished" (завершено)
- home_score / away_score: заполняются только если статус не "scheduled"
- updated_at: обновляется каждый раз когда меняется статус/результат

Зачем:
- Основная таблица со всеми событиями
- Связь с видами спорта и лигами
- Информация о статусе и результате
```

### ТАБЛИЦА 4: odds (Коэффициенты)

```sql
CREATE TABLE odds (
    odds_id SERIAL PRIMARY KEY,
    event_id INTEGER NOT NULL,
    bet_type VARCHAR(10) NOT NULL,
    coefficient DECIMAL(10,2) NOT NULL,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    FOREIGN KEY (event_id) REFERENCES events(event_id)
);

Данные:
odds_id | event_id | bet_type | coefficient | is_active | updated_at
1       | 1        | 1        | 1.85        | true      | 2025-12-15 10:30
2       | 1        | X        | 3.40        | true      | 2025-12-15 10:30
3       | 1        | 2        | 4.10        | true      | 2025-12-15 10:25
4       | 2        | 1        | 2.10        | true      | 2025-12-15 10:20
5       | 2        | X        | 3.50        | true      | 2025-12-15 10:20
6       | 2        | 2        | 3.20        | true      | 2025-12-15 10:20

Столбцы:
- bet_type: "1" (победа дома), "X" (ничья), "2" (победа в гостях)
- coefficient: 1.85, 2.10, 3.40 и т.д (коэффициент ставки)
- is_active: false если коэффициент больше не доступен

Зачем:
- Для каждого события может быть 3 коэффициента
- Коэффициенты могут меняться (обновляться)
- Можно отключить коэффициент если нужно
```

### ТАБЛИЦА 5: odds_history (История изменений коэффициентов)

```sql
CREATE TABLE odds_history (
    history_id SERIAL PRIMARY KEY,
    odds_id INTEGER NOT NULL,
    old_coefficient DECIMAL(10,2),
    new_coefficient DECIMAL(10,2),
    changed_at TIMESTAMP DEFAULT NOW(),
    changed_by VARCHAR(50),
    reason VARCHAR(200),
    FOREIGN KEY (odds_id) REFERENCES odds(odds_id)
);

Данные:
history_id | odds_id | old_coefficient | new_coefficient | changed_at           | changed_by | reason
1          | 1       | 1.85            | 1.90            | 2025-12-15 10:30:00 | admin_1    | Много ставок на П1
2          | 1       | 1.90            | 1.85            | 2025-12-15 11:15:00 | admin_2    | Коррекция
3          | 2       | 3.40            | 3.35            | 2025-12-15 11:20:00 | admin_1    | Балансировка

Зачем:
- Отслеживать как менялись коэффициенты
- Аудит (кто и когда изменил)
- История для анализа
```

### ЧТО ДОЛЖНО ПОЛУЧИТЬСЯ?

```
PostgreSQL база "looseline":

✅ sports_types (5 видов спорта)
✅ leagues (10+ лиг)
✅ events (200+ событий)
✅ odds (600+ коэффициентов)
✅ odds_history (лог изменений)

Все таблицы связаны через FOREIGN KEY
Готово для работы!
```

---

## 🔌 ЭТАП 2: СОЗДАНИЕ 5 КЛЮЧЕВЫХ МЕТОДОВ BACKEND (4-6 часов)

### ЧТО ЭТО?

Петр создаёт **5 методов (функций) на Python**, которые реализуют основную логику:

```
1️⃣ showStartMenu() — главное меню
2️⃣ loadSportEvents() — загрузка событий
3️⃣ filterEventsByType() — фильтрация по спорту
4️⃣ updateCoefficients() — обновление коэффициентов
5️⃣ manageSportEvents() — управление событиями (создание, редактирование, удаление)
```

---

## **МЕТОД 1: showStartMenu()**

### ЧТО ЭТО?

Метод показывает главное меню приложения.

### ЧТО ОН ДОЛЖЕН ВОЗВРАЩАТЬ?

```json
{
  "sports": [
    { "sport_id": 1, "name": "football", "emoji": "⚽", "event_count": 234 },
    { "sport_id": 2, "name": "basketball", "emoji": "🏀", "event_count": 156 },
    { "sport_id": 3, "name": "tennis", "emoji": "🎾", "event_count": 89 }
  ],
  "upcoming_events_24h": 45,
  "popular_leagues": [
    { "league_id": 1, "name": "Premier League", "sport_name": "football" },
    { "league_id": 3, "name": "NBA", "sport_name": "basketball" }
  ],
  "total_active_events": 234
}
```

### ЧТО ДЕЛАЕТ МЕТОД?

```
1. Подключается к БД (PostgreSQL)
   ↓
2. Выполняет SQL запрос:
   SELECT sport_id, sport_name, icon_emoji, 
          COUNT(*) as event_count
   FROM sports_types st
   LEFT JOIN events e ON st.sport_id = e.sport_id
   GROUP BY st.sport_id
   ↓
3. Выполняет второй запрос:
   SELECT COUNT(*) as upcoming_24h
   FROM events
   WHERE event_datetime BETWEEN NOW() AND NOW() + INTERVAL '24 hours'
   AND status = 'scheduled'
   ↓
4. Выполняет третий запрос:
   SELECT league_id, league_name, sport_id
   FROM leagues
   ORDER BY (SELECT COUNT(*) FROM events WHERE league_id = leagues.league_id) DESC
   LIMIT 5
   ↓
5. Возвращает всё это в виде JSON
```

### ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ:

```
Frontend вызывает:
GET /api/events/menu

Backend (Петр) обрабатывает:
showStartMenu()

Возвращает JSON с видами спорта, событиями на 24h, лигами

Frontend показывает:
┌──────────────────────────────┐
│ ⚽ Football (234 событий)     │
│ 🏀 Basketball (156 событий)   │
│ 🎾 Tennis (89 событий)        │
└──────────────────────────────┘
```

---

## **МЕТОД 2: loadSportEvents()**

### ЧТО ЭТО?

Метод загружает список событий с коэффициентами.

### ПАРАМЕТРЫ:

```python
def loadSportEvents(
    sport_type=None,        # "football" (опционально)
    league_name=None,       # "Premier League" (опционально)
    status=None,            # "scheduled", "live", "finished" (опционально)
    page=1,                 # номер страницы
    per_page=20             # событий на странице
):
```

### ЧТО ОН ДОЛЖЕН ВОЗВРАЩАТЬ?

```json
{
  "total_count": 234,
  "page": 1,
  "per_page": 20,
  "events": [
    {
      "event_id": 1,
      "sport_type": "football",
      "league": "Premier League",
      "home_team": "Manchester United",
      "away_team": "Liverpool",
      "event_datetime": "2025-12-15T18:00:00Z",
      "status": "scheduled",
      "home_score": null,
      "away_score": null,
      "odds": [
        { "odds_id": 1, "bet_type": "1", "coefficient": 1.85 },
        { "odds_id": 2, "bet_type": "X", "coefficient": 3.40 },
        { "odds_id": 3, "bet_type": "2", "coefficient": 4.10 }
      ]
    },
    {
      "event_id": 2,
      "sport_type": "football",
      "league": "La Liga",
      "home_team": "Real Madrid",
      "away_team": "Barcelona",
      "event_datetime": "2025-12-15T19:45:00Z",
      "status": "live",
      "home_score": 1,
      "away_score": 0,
      "odds": [
        { "odds_id": 4, "bet_type": "1", "coefficient": 2.10 },
        { "odds_id": 5, "bet_type": "X", "coefficient": 3.50 },
        { "odds_id": 6, "bet_type": "2", "coefficient": 3.20 }
      ]
    }
    // ... ещё 18 событий
  ]
}
```

### ЧТО ДЕЛАЕТ МЕТОД?

```
1. Подключается к БД
   ↓
2. Строит динамический SQL запрос:
   SELECT e.*, st.sport_name, l.league_name
   FROM events e
   JOIN sports_types st ON e.sport_id = st.sport_id
   JOIN leagues l ON e.league_id = l.league_id
   WHERE 1=1
   
   (затем добавляет условия):
   AND (st.sport_name = 'football' OR sport_type IS NULL)
   AND (l.league_name = 'Premier League' OR league_name IS NULL)
   AND (e.status = 'scheduled' OR status IS NULL)
   
   ORDER BY e.event_datetime ASC
   LIMIT 20 OFFSET (page-1)*20
   ↓
3. Для каждого события выполняет запрос:
   SELECT * FROM odds WHERE event_id = ?
   ↓
4. Собирает JSON структуру
   ↓
5. Возвращает
```

### ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ:

```
ПРИМЕР 1: Все события
Frontend вызывает:
GET /api/events/?page=1&per_page=20

Backend вызывает:
loadSportEvents(page=1, per_page=20)

Возвращает: все события (234 всего)

─────────────────────────────────────

ПРИМЕР 2: Только футбол
Frontend вызывает:
GET /api/events/?sport_type=football&page=1

Backend вызывает:
loadSportEvents(sport_type='football', page=1)

Возвращает: только футбольные события (67 всего)

─────────────────────────────────────

ПРИМЕР 3: Только Premier League
Frontend вызывает:
GET /api/events/?sport_type=football&league=Premier+League&page=1

Backend вызывает:
loadSportEvents(sport_type='football', league_name='Premier League', page=1)

Возвращает: только матчи Premier League (23 всего)

─────────────────────────────────────

ПРИМЕР 4: Только live события
Frontend вызывает:
GET /api/events/?status=live&page=1

Backend вызывает:
loadSportEvents(status='live', page=1)

Возвращает: только идущие сейчас события (12 всего)
```

---

## **МЕТОД 3: filterEventsByType()**

### ЧТО ЭТО?

Метод фильтрует события только по одному виду спорта.

### ПАРАМЕТРЫ:

```python
def filterEventsByType(
    sport_type: str         # "football", "basketball", "tennis" и т.д
):
```

### ЧТО ОН ДОЛЖЕН ВОЗВРАЩАТЬ?

```json
{
  "sport_type": "football",
  "total_events": 67,
  "events": [
    // Все события этого спорта с коэффициентами
  ],
  "leagues": [
    { "league_id": 1, "name": "Premier League", "count": 23 },
    { "league_id": 2, "name": "La Liga", "count": 19 },
    { "league_id": 5, "name": "Serie A", "count": 25 }
  ]
}
```

### ЧТО ДЕЛАЕТ МЕТОД?

```
1. Подключается к БД
   ↓
2. Проверяет существует ли такой спорт:
   SELECT sport_id FROM sports_types 
   WHERE sport_name = 'football'
   
   Если не существует → возвращает ошибку 404
   ↓
3. Выполняет запрос:
   SELECT e.*, st.sport_name, l.league_name
   FROM events e
   JOIN sports_types st ON e.sport_id = st.sport_id
   JOIN leagues l ON e.league_id = l.league_id
   WHERE st.sport_name = 'football'
   ORDER BY e.event_datetime ASC
   ↓
4. Для каждого события загружает коэффициенты
   ↓
5. Загружает список лиг этого спорта:
   SELECT league_id, league_name, COUNT(*) as count
   FROM leagues l
   LEFT JOIN events e ON l.league_id = e.league_id
   WHERE l.sport_id = (SELECT sport_id FROM sports_types WHERE sport_name = 'football')
   GROUP BY league_id
   ↓
6. Возвращает JSON
```

### ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ:

```
Frontend вызывает:
GET /api/events/filter/football

Backend вызывает:
filterEventsByType('football')

Возвращает:
{
  "sport_type": "football",
  "total_events": 67,
  "events": [// 67 футбольных матчей],
  "leagues": [
    {"name": "Premier League", "count": 23},
    {"name": "La Liga", "count": 19},
    {"name": "Serie A", "count": 25}
  ]
}

Frontend показывает:
- Список всех футбольных матчей
- Выпадающее меню с лигами
- Количество матчей в каждой лиге
```

---

## **МЕТОД 4: updateCoefficients()**

### ЧТО ЭТО?

Метод обновляет коэффициент для конкретной ставки (только для админов).

### ПАРАМЕТРЫ:

```python
def updateCoefficients(
    odds_id: int,           # ID коэффициента
    new_coefficient: float, # новое значение (1.85, 2.10 и т.д)
    admin_id: str,          # ID администратора
    reason: str = None      # причина изменения
):
```

### ЧТО ОН ДОЛЖЕН ВОЗВРАЩАТЬ?

```json
{
  "success": true,
  "message": "Коэффициент обновлён с 1.85 на 1.90",
  "odds": {
    "odds_id": 1,
    "event_id": 1,
    "bet_type": "1",
    "old_coefficient": 1.85,
    "new_coefficient": 1.90,
    "updated_at": "2025-12-15T10:30:00Z"
  },
  "history_entry": {
    "history_id": 1,
    "odds_id": 1,
    "old_coefficient": 1.85,
    "new_coefficient": 1.90,
    "changed_by": "admin_1",
    "reason": "Много ставок на П1"
  }
}
```

### ЧТО ДЕЛАЕТ МЕТОД?

```
1. Проверяет существует ли коэффициент:
   SELECT * FROM odds WHERE odds_id = ?
   
   Если не существует → возвращает ошибку 404
   ↓
2. Проверяет есть ли права админа:
   SELECT * FROM users WHERE id = ? AND role = 'admin'
   
   Если не админ → возвращает ошибку 403 (доступ запрещен)
   ↓
3. Сохраняет старое значение в переменную
   ↓
4. Проверяет валидность нового коэффициента:
   if new_coefficient < 1.01:
       return error("Коэффициент не может быть < 1.01")
   if new_coefficient > 100:
       return error("Коэффициент не может быть > 100")
   ↓
5. Обновляет коэффициент:
   UPDATE odds 
   SET coefficient = ?, updated_at = NOW()
   WHERE odds_id = ?
   ↓
6. Записывает в историю:
   INSERT INTO odds_history 
   (odds_id, old_coefficient, new_coefficient, changed_by, reason)
   VALUES (?, ?, ?, ?, ?)
   ↓
7. Возвращает JSON с успехом
```

### ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ:

```
ПРИМЕР: Админ меняет коэффициент
Frontend (админ-панель) вызывает:
POST /api/events/odds/1/update
{
  "new_coefficient": 1.90,
  "reason": "Много ставок на П1"
}

Backend вызывает:
updateCoefficients(
    odds_id=1,
    new_coefficient=1.90,
    admin_id="admin_1",
    reason="Много ставок на П1"
)

Backend обрабатывает:
1. Проверяет есть ли коэффициент #1
2. Проверяет права админа
3. Проверяет 1.01 < 1.90 < 100 ✅
4. Обновляет в таблице odds
5. Записывает в odds_history
6. Возвращает успех

Frontend показывает:
"✅ Коэффициент обновлён с 1.85 на 1.90"

База данных теперь содержит:
odds: 1.90
odds_history: 1.85 → 1.90 (админ_1, "Много ставок на П1")
```

---

## **МЕТОД 5: manageSportEvents()**

### ЧТО ЭТО?

Метод управляет событиями: создание, редактирование, удаление (только для админов).

### ПАРАМЕТРЫ:

```python
def manageSportEvents(
    action: str,            # "create", "update", "delete"
    event_id: int = None,   # нужен для update/delete
    admin_id: str = None,   # ID админа
    **kwargs                # остальные параметры зависят от action
):
```

### ПОДМЕТОД 5A: Создание события (action="create")

**Параметры:**

```python
manageSportEvents(
    action="create",
    sport_type="football",
    league_name="Premier League",
    home_team="Manchester United",
    away_team="Liverpool",
    event_datetime="2025-12-15T18:00:00Z",
    odds_data=[
        {"bet_type": "1", "coefficient": 1.85},
        {"bet_type": "X", "coefficient": 3.40},
        {"bet_type": "2", "coefficient": 4.10}
    ],
    admin_id="admin_1"
)
```

**Что он делает:**

```
1. Проверяет права админа
   ↓
2. Проверяет существует ли спорт:
   SELECT sport_id FROM sports_types WHERE sport_name = 'football'
   
   Если не существует → возвращает ошибку
   ↓
3. Проверяет существует ли лига:
   SELECT league_id FROM leagues WHERE league_name = 'Premier League'
   
   Если не существует → возвращает ошибку
   ↓
4. Проверяет что event_datetime в будущем:
   if event_datetime <= NOW():
       return error("Событие не может быть в прошлом")
   ↓
5. Проверяет что teams разные:
   if home_team == away_team:
       return error("Домашняя и гостевая команды не могут быть одинаковыми")
   ↓
6. Создаёт событие:
   INSERT INTO events 
   (sport_id, league_id, home_team, away_team, event_datetime, status)
   VALUES (?, ?, ?, ?, ?, 'scheduled')
   
   Получает event_id
   ↓
7. Создаёт коэффициенты:
   для каждого коэффициента в odds_data:
   INSERT INTO odds 
   (event_id, bet_type, coefficient, is_active)
   VALUES (?, ?, ?, TRUE)
   ↓
8. Возвращает JSON с новым событием
```

**Возвращает:**

```json
{
  "success": true,
  "message": "Событие создано",
  "event": {
    "event_id": 1,
    "sport_type": "football",
    "league": "Premier League",
    "home_team": "Manchester United",
    "away_team": "Liverpool",
    "event_datetime": "2025-12-15T18:00:00Z",
    "status": "scheduled",
    "odds": [
      {"odds_id": 1, "bet_type": "1", "coefficient": 1.85},
      {"odds_id": 2, "bet_type": "X", "coefficient": 3.40},
      {"odds_id": 3, "bet_type": "2", "coefficient": 4.10}
    ]
  }
}
```

---

### ПОДМЕТОД 5B: Редактирование события (action="update")

**Параметры:**

```python
manageSportEvents(
    action="update",
    event_id=1,
    status="live",           # changed от scheduled к live
    home_score=1,
    away_score=0,
    admin_id="admin_1"
)
```

**Что он делает:**

```
1. Проверяет права админа
   ↓
2. Проверяет существует ли событие:
   SELECT * FROM events WHERE event_id = 1
   
   Если не существует → возвращает ошибку 404
   ↓
3. Если меняется статус (scheduled → live):
   - Проверяет что можно только в scheduled/live переходить
   - Если переход с live на finished → требует результаты (score)
   ↓
4. Если меняется результат (home_score, away_score):
   - Проверяет что статус не "scheduled"
   - Проверяет что score неотрицательные
   ↓
5. Обновляет событие:
   UPDATE events 
   SET status = ?, home_score = ?, away_score = ?, updated_at = NOW()
   WHERE event_id = ?
   ↓
6. Если статус изменился на "finished":
   - Закрывает все ставки на это событие
   - Расчитывает выигрыши/проигрыши (через модуль Третьего)
   ↓
7. Возвращает JSON с обновленным событием
```

**Возвращает:**

```json
{
  "success": true,
  "message": "Событие обновлено",
  "event": {
    "event_id": 1,
    "status": "live",
    "home_score": 1,
    "away_score": 0,
    "updated_at": "2025-12-15T10:30:00Z"
  }
}
```

---

### ПОДМЕТОД 5C: Удаление события (action="delete")

**Параметры:**

```python
manageSportEvents(
    action="delete",
    event_id=1,
    admin_id="admin_1",
    reason="Матч отменён"
)
```

**Что он делает:**

```
1. Проверяет права админа
   ↓
2. Проверяет существует ли событие
   ↓
3. Проверяет статус события:
   - Если status = "finished" → ошибка "Нельзя удалять завершённые события"
   - Если status = "live" → ошибка "Нельзя удалять идущие события"
   - Если status = "scheduled" → можно удалять
   ↓
4. Проверяет есть ли на это событие ставки:
   SELECT COUNT(*) FROM bets WHERE event_id = ?
   
   Если есть ставки → спрашивает админа что делать:
   - Аннулировать ставки (вернуть деньги)
   - Отменить удаление
   ↓
5. Удаляет коэффициенты:
   DELETE FROM odds WHERE event_id = ?
   ↓
6. Удаляет событие:
   DELETE FROM events WHERE event_id = ?
   ↓
7. Если были ставки → возвращает деньги пользователям
   (через модуль Третьего)
   ↓
8. Возвращает JSON с успехом
```

**Возвращает:**

```json
{
  "success": true,
  "message": "Событие удалено",
  "deleted_event_id": 1,
  "bets_processed": 0,
  "money_returned": 0.00
}
```

---

## 📊 ЭТАП 3: ДОКУМЕНТИРОВАНИЕ КОДА (1-2 часа)

### ЧТО ПЕТР ДОЛЖЕН НАПИСАТЬ?

**Для каждого метода:**

```python
def loadSportEvents(
    sport_type: str = None,
    league_name: str = None,
    status: str = None,
    page: int = 1,
    per_page: int = 20
) -> dict:
    """
    Загружает список спортивных событий с фильтрацией.
    
    Args:
        sport_type (str, optional): Вид спорта ('football', 'basketball').
            Если None, возвращает все виды. Defaults to None.
        league_name (str, optional): Название лиги ('Premier League').
            Если None, возвращает все лиги. Defaults to None.
        status (str, optional): Статус события ('scheduled', 'live', 'finished').
            Если None, возвращает все статусы. Defaults to None.
        page (int): Номер страницы для пагинации. Defaults to 1.
        per_page (int): Количество событий на странице. Defaults to 20.
    
    Returns:
        dict: Словарь с ключами:
            - total_count (int): Всего событий в БД с учётом фильтров
            - page (int): Текущая страница
            - per_page (int): События на странице
            - events (list): Список событий с коэффициентами
    
    Raises:
        ValueError: Если sport_type не существует в БД
        ValueError: Если league_name не существует в БД
        ValueError: Если page < 1 или per_page > 100
        DatabaseError: Если ошибка подключения к БД
    
    Examples:
        >>> # Загрузить всё события
        >>> events = loadSportEvents(page=1, per_page=20)
        >>> print(events['total_count'])
        234
        
        >>> # Загрузить только футбол
        >>> football = loadSportEvents(sport_type='football')
        >>> len(football['events'])
        20
        
        >>> # Загрузить live события
        >>> live = loadSportEvents(status='live')
        >>> print(f"Идёт сейчас: {live['total_count']} событий")
        Идёт сейчас: 12 событий
    
    Database Queries:
        1. SELECT * FROM events WHERE ... (с фильтрами)
        2. SELECT * FROM odds WHERE event_id = ? (для каждого события)
        3. SELECT COUNT(*) FROM events (для total_count)
    """
```

### ЧТО ЕЩЁ ДОКУМЕНТИРОВАТЬ?

-  **API документация** (какие параметры, какой возвращает результат)

-  **Примеры использования** для каждого метода

-  **Ошибки** которые могут возникнуть и как их обрабатывать

-  **Производительность** (сколько занимает запрос)

-  **Ограничения** (максимум записей, максимум на странице)

---

## ✅ ЭТАП 4: ТЕСТИРОВАНИЕ (2-3 часа)

Минимум **3 тестовых сценария** для каждого метода.

### ТЕСТИРОВАНИЕ МЕТОДА: loadSportEvents()

**Тест 1: Загрузить все события**

```
ШАГ 1: Предусловие
- База содержит 234 события
- 67 из них футбол
- 156 баскетбол
- 89 теннис

ШАГ 2: Действие
loadSportEvents(page=1, per_page=20)

ШАГ 3: Ожидаемый результат
{
  "total_count": 234,
  "page": 1,
  "per_page": 20,
  "events": [20 событий]
}

ШАГ 4: Проверка
✅ total_count = 234
✅ len(events) = 20
✅ У каждого события есть коэффициенты (3 шт)
✅ События отсортированы по event_datetime
```

**Тест 2: Фильтр по спорту (только футбол)**

```
ШАГ 1: Предусловие
- 67 футбольных событий в БД

ШАГ 2: Действие
loadSportEvents(sport_type='football', page=1, per_page=20)

ШАГ 3: Ожидаемый результат
{
  "total_count": 67,
  "page": 1,
  "per_page": 20,
  "events": [20 футбольных событий]
}

ШАГ 4: Проверка
✅ total_count = 67 (только футбол)
✅ len(events) = 20
✅ Все события имеют sport_type = 'football'
✅ Нет событий других видов спорта
```

**Тест 3: Пагинация (2-я страница)**

```
ШАГ 1: Предусловие
- 234 события, по 20 на странице = 12 страниц

ШАГ 2: Действие
loadSportEvents(page=2, per_page=20)

ШАГ 3: Ожидаемый результат
События с 21 по 40

ШАГ 4: Проверка
✅ page = 2
✅ len(events) = 20
✅ Первое событие имеет ID 21
✅ Последнее событие имеет ID 40
```

**Тест 4: Некорректный спорт (ошибка)**

```
ШАГ 1: Предусловие
- Спорт 'underwater_basket' не существует в БД

ШАГ 2: Действие
loadSportEvents(sport_type='underwater_basket')

ШАГ 3: Ожидаемый результат
Ошибка 400:
{
  "error": "Sport type 'underwater_basket' not found"
}

ШАГ 4: Проверка
✅ Вернулась ошибка
✅ Статус кода = 400
✅ Сообщение понятное
```

---

### ТЕСТИРОВАНИЕ МЕТОДА: updateCoefficients()

**Тест 1: Успешное обновление коэффициента**

```
ШАГ 1: Предусловие
- Коэффициент #1 имеет значение 1.85
- Админ с ID "admin_1" существует

ШАГ 2: Действие
updateCoefficients(
    odds_id=1,
    new_coefficient=1.90,
    admin_id="admin_1",
    reason="Много ставок"
)

ШАГ 3: Ожидаемый результат
{
  "success": true,
  "message": "Коэффициент обновлён",
  "odds": {
    "odds_id": 1,
    "old_coefficient": 1.85,
    "new_coefficient": 1.90
  }
}

ШАГ 4: Проверка
✅ odds.coefficient = 1.90 в БД
✅ В odds_history добавлена запись (1.85 → 1.90)
✅ changed_by = "admin_1"
✅ reason = "Много ставок"
```

**Тест 2: Коэффициент невалиден (слишком маленький)**

```
ШАГ 1: Предусловие
- Коэффициент не может быть < 1.01

ШАГ 2: Действие
updateCoefficients(
    odds_id=1,
    new_coefficient=0.99,
    admin_id="admin_1"
)

ШАГ 3: Ожидаемый результат
Ошибка 400:
{
  "error": "Coefficient must be >= 1.01"
}

ШАГ 4: Проверка
✅ Коэффициент НЕ изменился в БД
✅ odds_history не добавилась запись
✅ Вернулась понятная ошибка
```

**Тест 3: Нет прав админа (ошибка доступа)**

```
ШАГ 1: Предусловие
- Пользователь "user_123" имеет role = 'user' (не админ)

ШАГ 2: Действие
updateCoefficients(
    odds_id=1,
    new_coefficient=1.90,
    admin_id="user_123"
)

ШАГ 3: Ожидаемый результат
Ошибка 403:
{
  "error": "Permission denied. Only admins can update odds."
}

ШАГ 4: Проверка
✅ Коэффициент НЕ изменился
✅ Вернулась ошибка 403 (Forbidden)
✅ Логирование: попытка несанкционированного доступа
```

---

### ТЕСТИРОВАНИЕ МЕТОДА: manageSportEvents() - CREATE

**Тест 1: Успешное создание события**

```
ШАГ 1: Предусловие
- Спорт 'football' существует
- Лига 'Premier League' существует
- Дата события в будущем

ШАГ 2: Действие
manageSportEvents(
    action='create',
    sport_type='football',
    league_name='Premier League',
    home_team='Chelsea',
    away_team='Tottenham',
    event_datetime='2025-12-20T15:00:00Z',
    odds_data=[
        {"bet_type": "1", "coefficient": 1.80},
        {"bet_type": "X", "coefficient": 3.50},
        {"bet_type": "2", "coefficient": 4.20}
    ],
    admin_id='admin_1'
)

ШАГ 3: Ожидаемый результат
{
  "success": true,
  "event": {
    "event_id": 235,
    "home_team": "Chelsea",
    "away_team": "Tottenham",
    "status": "scheduled",
    "odds": [3 коэффициента]
  }
}

ШАГ 4: Проверка
✅ В таблице events появилась новая запись (ID 235)
✅ В таблице odds появилось 3 коэффициента
✅ event_id = 235 в odds совпадает
✅ status = 'scheduled'
```

**Тест 2: Команды одинаковые (ошибка)**

```
ШАГ 1: Действие
manageSportEvents(
    action='create',
    sport_type='football',
    league_name='Premier League',
    home_team='Chelsea',
    away_team='Chelsea',  # ← ОШИБКА!
    ...
)

ШАГ 2: Ожидаемый результат
Ошибка 400:
{
  "error": "Home team and away team cannot be the same"
}

ШАГ 3: Проверка
✅ Событие НЕ создано
✅ Коэффициенты НЕ созданы
```

**Тест 3: Дата в прошлом (ошибка)**

```
ШАГ 1: Действие
manageSportEvents(
    action='create',
    ...
    event_datetime='2025-12-10T15:00:00Z',  # дата прошлого месяца
    ...
)

ШАГ 2: Ожидаемый результат
Ошибка 400:
{
  "error": "Event datetime must be in the future"
}

ШАГ 3: Проверка
✅ Событие НЕ создано
```

---

## 📊 ЭТАП 5: ТАБЛИЦА РЕЗУЛЬТАТОВ (1 час)

Таблица в документации со статусом каждого теста.

# РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ МОДУЛЯ ПЕТРА

## МЕТОД 1: showStartMenu()

| Тест | Описание | Статус | Примечание |
|------|---------|--------|-----------|
| 1 | Загрузка меню при старте | ✅ PASS | Время: 45ms |
| 2 | Показать события на 24h | ✅ PASS | Корректное количество |
| 3 | Популярные лиги сортируются по кол-во событий | ✅ PASS | Порядок правильный |

**Итого: 3/3 тестов пройдено (100%)**

---

## МЕТОД 2: loadSportEvents()

| Тест | Описание | Статус | Примечание |
|------|---------|--------|-----------|
| 1 | Загрузить все события (page=1) | ✅ PASS | 20 событий вернулось |
| 2 | Фильтр по спорту (футбол) | ✅ PASS | Только 67 событий |
| 3 | Пагинация page=2 | ✅ PASS | События 21-40 |
| 4 | Некорректный спорт (ошибка) | ✅ PASS | Ошибка 400 вернулась |

**Итого: 4/4 тестов пройдено (100%)**

---

## МЕТОД 3: filterEventsByType()

| Тест | Описание | Статус | Примечание |
|------|---------|--------|-----------|
| 1 | Фильтр по спорту 'football' | ✅ PASS | 67 событий |
| 2 | Список лиг загружается | ✅ PASS | 5 лиг в ответе |
| 3 | Некорректный спорт (ошибка) | ✅ PASS | Ошибка 404 |

**Итого: 3/3 тестов пройдено (100%)**

---

## МЕТОД 4: updateCoefficients()

| Тест | Описание | Статус | Примечание |
|------|---------|--------|-----------|
| 1 | Успешное обновление (1.85 → 1.90) | ✅ PASS | История сохранена |
| 2 | Коэффициент < 1.01 (ошибка) | ✅ PASS | Ошибка 400 |
| 3 | Нет прав админа (ошибка) | ✅ PASS | Ошибка 403 |
| 4 | Коэффициент > 100 (ошибка) | ✅ PASS | Ошибка 400 |

**Итого: 4/4 тестов пройдено (100%)**

---

## МЕТОД 5: manageSportEvents()

### ПОДМЕТОД: CREATE

| Тест | Описание | Статус | Примечание |
|------|---------|--------|-----------|
| 1 | Создание события (Chelsea vs Tottenham) | ✅ PASS | event_id = 235 |
| 2 | 3 коэффициента созданы | ✅ PASS | odds_id: 1, 2, 3 |
| 3 | Команды одинаковые (ошибка) | ✅ PASS | Ошибка 400 |
| 4 | Дата в прошлом (ошибка) | ✅ PASS | Ошибка 400 |

**Итого: 4/4 тестов пройдено (100%)**

### ПОДМЕТОД: UPDATE

| Тест | Описание | Статус | Примечание |
|------|---------|--------|-----------|
| 1 | Изменить статус (scheduled → live) | ✅ PASS | Обновлено |
| 2 | Установить результат (1-0) | ✅ PASS | home_score=1 |
| 3 | Завершить событие (live → finished) | ✅ PASS | Ставки закрыты |
| 4 | Результат без финального статуса (ошибка) | ✅ PASS | Ошибка 400 |

**Итого: 4/4 тестов пройдено (100%)**

### ПОДМЕТОД: DELETE

| Тест | Описание | Статус | Примечание |
|------|---------|--------|-----------|
| 1 | Удаление scheduled события | ✅ PASS | Событие удалено |
| 2 | Удаление live события (ошибка) | ✅ PASS | Ошибка 400 |
| 3 | Возврат денег при удалении | ✅ PASS | Пользователи получили |

**Итого: 3/3 тестов пройдено (100%)**

---

## ИТОГОВАЯ СТАТИСТИКА

| Метод | Тестов | Пройдено | Ошибок | % Успеха |
|-------|--------|----------|--------|----------|
| showStartMenu() | 3 | 3 | 0 | 100% |
| loadSportEvents() | 4 | 4 | 0 | 100% |
| filterEventsByType() | 3 | 3 | 0 | 100% |
| updateCoefficients() | 4 | 4 | 0 | 100% |
| manageSportEvents() | 11 | 11 | 0 | 100% |
| **ВСЕГО** | **25** | **25** | **0** | **100%** |

## ПРОИЗВОДИТЕЛЬНОСТЬ

| Метод | Среднее время | Макс. время | Мин. время |
|-------|--------------|-------------|-----------|
| showStartMenu() | 45ms | 52ms | 38ms |
| loadSportEvents() | 85ms | 120ms | 65ms |
| filterEventsByType() | 92ms | 145ms | 78ms |
| updateCoefficients() | 25ms | 35ms | 18ms |
| manageSportEvents() CREATE | 140ms | 185ms | 105ms |
| manageSportEvents() UPDATE | 95ms | 130ms | 72ms |
| manageSportEvents() DELETE | 110ms | 160ms | 85ms |

## ВЫВОДЫ

✅ Все методы работают корректно
✅ Все ошибки обрабатываются правильно
✅ Производительность в норме (все запросы < 200ms)
✅ Код задокументирован
✅ Готово к production

---

## 🎯 ФИНАЛЬНЫЙ ЧЕК-ЛИСТ

### ТАБЛИЦЫ БД:

* [ ] sports_types (5 записей минимум)

* [ ] leagues (10+ записей)

* [ ] events (200+ записей)

* [ ] odds (600+ записей)

* [ ] odds_history (логирование изменений)

### МЕТОДЫ:

* [ ] showStartMenu() -- полная реализация

* [ ] loadSportEvents() -- полная реализация

* [ ] filterEventsByType() -- полная реализация

* [ ] updateCoefficients() -- полная реализация

* [ ] manageSportEvents() -- полная реализация (create, update, delete)

### ДОКУМЕНТИРОВАНИЕ:

* [ ] Docstring для каждого метода

* [ ] Примеры использования

* [ ] Описание параметров и возврата

* [ ] Описание ошибок

* [ ] Database queries документированы

### ТЕСТИРОВАНИЕ:

* [ ] 3+ тестовых сценария для каждого метода

* [ ] Позитивные тесты (всё работает)

* [ ] Негативные тесты (ошибки обрабатываются)

* [ ] Edge cases (граничные случаи)

* [ ] Таблица результатов в документации

### КАЧЕСТВО КОДА:

* [ ] Нет SQL injection уязвимостей

* [ ] Проверка прав доступа (только админ может редактировать)

* [ ] Валидация всех входных данных

* [ ] Обработка ошибок БД

* [ ] Логирование действий

### API ENDPOINTS:

* [ ] GET /api/events/menu

* [ ] GET /api/events/?sport_type=...&league=...&page=...

* [ ] GET /api/events/filter/\{sport_type}

* [ ] POST /api/events/odds/\{odds_id}/update

* [ ] POST /api/events/ (создание)

* [ ] PUT /api/events/\{event_id} (обновление)

* [ ] DELETE /api/events/\{event_id} (удаление)

---

## 📌 КРАТКОЕ РЕЗЮМЕ

1. **5 таблиц БД** (sports_types, leagues, events, odds, odds_history)

2. **5 ключевых методов:**

   -  showStartMenu() -> показывает виды спорта и события на 24h

   -  loadSportEvents() -> загружает события с фильтром и пагинацией

   -  filterEventsByType() -> фильтр по одному спорту

   -  updateCoefficients() -> обновляет коэффициенты (админ)

   -  manageSportEvents() -> управление событиями (админ)

3. **Документировать** весь код с примерами

4. **Протестировать** минимум 25 тестовых сценариев

5. **Создать таблицу результатов** в документации

**Результат:** Полностью функциональный Backend модуль для спортивных событий! 🚀