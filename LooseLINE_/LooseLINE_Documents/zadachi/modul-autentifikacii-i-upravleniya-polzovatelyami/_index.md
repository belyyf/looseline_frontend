---
order: 1
title: Модуль аутентификации и управления пользователями
---

### **Нужно подключить это:** <https://github.com/better-auth/better-auth>

**Better-auth** -- это готовая библиотека для:

-  Регистрации пользователей

-  Авторизации (вход)

-  Управления сессиями (логины и логауты)

-  OAuth (вход через Google, GitHub, т.д.)

-  Двухфакторной аутентификации (2FA)

-  Восстановления пароля

**Главное:** он автоматически создаёт таблицы в БД и предоставляет готовые компоненты UI!

---

## 🏗️ ЭТАП 1: УСТАНОВКА BETTER-AUTH (30 минут)

### ЧТО ДЕЛАТЬ?

**Шаг 1:** Установить пакет

```
npm install better-auth
```

**Шаг 2:** Установить адаптер для PostgreSQL

```
npm install @better-auth/pg
```

**Шаг 3:** Установить UI компоненты (если используются)

```
npm install @better-auth/react
```

### ЧТО ДОЛЖНО ПОЛУЧИТЬСЯ?

```
В папке node_modules/ появятся папки:
✅ better-auth/
✅ @better-auth/pg/
✅ @better-auth/react/

Готовые компоненты и функции для авторизации
```

---

## 🗄️ ЭТАП 2: ТАБЛИЦЫ БД ОТ BETTER-AUTH (1-2 часа)

### ЧТО ПРОИСХОДИТ?

Better-auth **автоматически создаёт таблицы в PostgreSQL**:

```
📊 ТАБЛИЦА 1: users
├── id (уникальный ID)
├── email (электронная почта)
├── emailVerified (проверена ли почта)
├── name (имя пользователя)
├── image (фото профиля)
├── createdAt (дата создания)
└── updatedAt (последнее обновление)

📊 ТАБЛИЦА 2: sessions
├── id (ID сессии)
├── userId (ID пользователя)
├── token (уникальный токен)
├── expiresAt (когда сессия истекает)
├── createdAt
└── updatedAt

📊 ТАБЛИЦА 3: accounts
├── id (ID аккаунта)
├── userId (ID пользователя)
├── provider (где был вход: email, google, github)
├── providerAccountId (ID от провайдера)
├── accessToken (токен доступа)
├── refreshToken (токен обновления)
└── expiresAt (когда токен истекает)

📊 ТАБЛИЦА 4: verifications
├── id
├── identifier (email или номер телефона)
├── value (код для верификации)
├── expiresAt
└── createdAt

📊 ТАБЛИЦА 5: passwordResets
├── id
├── userId
├── email
├── token (код для восстановления)
├── expiresAt
└── createdAt
```

### ЧТО ДЕЛАТЬ?

**Шаг 1:** В файле `backend/app/main.py` инициализировать better-auth:

```python
# Импортировать better-auth
from better_auth import BetterAuth
from better_auth.adapters import PostgreSQL

# Создать инстанс
auth = BetterAuth(
    adapter=PostgreSQL(
        connection_string="postgresql://user:password@localhost/looseline"
    ),
    secret_key="ваш-секретный-ключ-тут"
)

# При первом запуске better-auth автоматически создаст таблицы
```

**Шаг 2:** При запуске Backend:

-  Better-auth проверит есть ли таблицы

-  Если нет -> создаст их

-  Если есть -> обновит их структуру (миграции)

### ЧТО ДОЛЖНО ПОЛУЧИТЬСЯ?

```
PostgreSQL база данных:

✅ users (поле email UNIQUE)
✅ sessions (связь с users)
✅ accounts (социальные логины)
✅ verifications (подтверждение почты)
✅ passwordResets (восстановление пароля)

Всё готово к работе!
```

---

## 🔌 ЭТАП 3: BACKEND API ENDPOINTS ДЛЯ АВТОРИЗАЦИИ (2-3 часа)

### ЧТО ЭТО?

Better-auth предоставляет готовые endpoints для авторизации.

### КАКИЕ ENDPOINTS БУДУТ?

```
1️⃣ РЕГИСТРАЦИЯ
POST /api/auth/sign-up
Параметры: email, password, name
Возвращает: { user, session, token }

2️⃣ ВХОД
POST /api/auth/sign-in
Параметры: email, password
Возвращает: { user, session, token }

3️⃣ ВЫХОД
POST /api/auth/sign-out
Параметры: token
Возвращает: { success: true }

4️⃣ ПОЛУЧИТЬ ТЕКУЩЕГО ПОЛЬЗОВАТЕЛЯ
GET /api/auth/session
Параметры: token (в header Authorization)
Возвращает: { user, session }

5️⃣ ОБНОВИТЬ ПРОФИЛЬ
PUT /api/auth/user
Параметры: name, image
Возвращает: { user }

6️⃣ ИЗМЕНИТЬ ПАРОЛЬ
POST /api/auth/change-password
Параметры: oldPassword, newPassword
Возвращает: { success: true }

7️⃣ ЗАБЫЛИ ПАРОЛЬ
POST /api/auth/forgot-password
Параметры: email
Возвращает: { success: true, message: "Проверьте почту" }

8️⃣ ВОССТАНОВИТЬ ПАРОЛЬ
POST /api/auth/reset-password
Параметры: token, newPassword
Возвращает: { success: true }

9️⃣ ПОДТВЕРЖДЕНИЕ EMAIL
POST /api/auth/verify-email
Параметры: code
Возвращает: { success: true }

🔟 ВХОД ЧЕРЕЗ GOOGLE
GET /api/auth/google/callback
(Better-auth сам обрабатывает)
```

### ЧТО ДЕЛАТЬ?

**Шаг 1:** Инициализировать эти endpoints в Backend:

```python
# Все endpoints автоматически создаются при:
auth = BetterAuth(...)

# Они готовы к использованию!
# Не нужно ничего писать, всё есть в better-auth
```

**Шаг 2:** Опционально добавить свои правила:

-  Минимальная длина пароля (8 символов)

-  Формат email

-  Какие поля обязательны при регистрации

### ЧТО ДОЛЖНО ПОЛУЧИТЬСЯ?

```
Backend готов:
✅ Принимает запросы на регистрацию
✅ Проверяет пароли и email
✅ Создаёт пользователя в БД
✅ Создаёт сессию
✅ Возвращает токен авторизации

Пример ответа при регистрации:
{
  "user": {
    "id": "user_123",
    "email": "petrov@mail.com",
    "name": "Петр",
    "image": null
  },
  "session": {
    "id": "session_456",
    "expiresAt": "2025-12-29T03:35:00Z"
  },
  "token": "eyJhbGciOiJIUzI1NiIs..."
}
```

---

## 🎨 ЭТАП 4: FRONTEND ИНТЕРФЕЙС (СТРАНИЦА АВТОРИЗАЦИИ) (3-4 часа)

### ЧТО НУЖНО СОЗДАТЬ?

Better-auth предоставляет **готовые React компоненты UI**!

### КОМПОНЕНТ 1: ФОРМА РЕГИСТРАЦИИ (Sign Up)

**ЧТО ЭТО?** Форма для создания нового аккаунта

**ЧТО ДОЛЖНА ПОКАЗЫВАТЬ?**

```
┌─────────────────────────────────┐
│  LOOSELINE - РЕГИСТРАЦИЯ         │
├─────────────────────────────────┤
│                                 │
│ Полное имя: [________________]   │
│ Электронная почта: [____________]│
│ Пароль: [____________________]  │
│ Повторить пароль: [____________]│
│                                 │
│ ☐ Согласен с условиями         │
│                                 │
│ [🟢 Зарегистрироваться]         │
│                                 │
│ Уже есть аккаунт? [Войти]       │
│                                 │
└─────────────────────────────────┘
```

**ЧТО ПРОИСХОДИТ?**

```
Пользователь заполняет форму
  ↓
Кликает "Зарегистрироваться"
  ↓
Frontend вызывает:
POST /api/auth/sign-up
{ email, password, name }
  ↓
Better-auth обрабатывает:
- Проверяет email (валиден ли)
- Проверяет пароль (не слабый ли)
- Хеширует пароль (SHA-256)
- Создаёт пользователя в таблице users
  ↓
Возвращает токен и данные пользователя
  ↓
Frontend сохраняет токен в localStorage
  ↓
Переходит на главную страницу
```

### КОМПОНЕНТ 2: ФОРМА ВХОДА (Sign In)

**ЧТО ЭТО?** Форма для входа в существующий аккаунт

**ЧТО ДОЛЖНА ПОКАЗЫВАТЬ?**

```
┌─────────────────────────────────┐
│  LOOSELINE - ВХОД                │
├─────────────────────────────────┤
│                                 │
│ Электронная почта: [____________]│
│ Пароль: [____________________]  │
│                                 │
│ ☐ Запомнить меня               │
│                                 │
│ [🟢 Войти]                      │
│                                 │
│ Забыли пароль? [Восстановить]  │
│ Нет аккаунта? [Регистрация]    │
│                                 │
│ ─ или войдите через ─          │
│                                 │
│ [🔵 Google]  [⚫ GitHub]        │
│                                 │
└─────────────────────────────────┘
```

**ЧТО ПРОИСХОДИТ?**

```
Пользователь вводит email и пароль
  ↓
Кликает "Войти"
  ↓
Frontend вызывает:
POST /api/auth/sign-in
{ email, password }
  ↓
Better-auth обрабатывает:
- Проверяет есть ли такой email в users
- Сравнивает пароль с хешем в БД
- Если ок → создаёт новую сессию
- Если не ок → возвращает ошибку
  ↓
Возвращает токен и сессию
  ↓
Frontend сохраняет токен
  ↓
Переходит на главную страницу
```

### КОМПОНЕНТ 3: ФОРМА ВОССТАНОВЛЕНИЯ ПАРОЛЯ

**ЧТО ДОЛЖНА ПОКАЗЫВАТЬ?**

```
ШАГ 1: Ввести email
┌──────────────────────────┐
│ ВОССТАНОВЛЕНИЕ ПАРОЛЯ    │
├──────────────────────────┤
│                          │
│ Email: [________________]│
│                          │
│ [Отправить код]          │
│ [Вернуться на вход]      │
│                          │
└──────────────────────────┘

ШАГ 2: Ввести код и новый пароль
┌──────────────────────────┐
│ ВВЕДИТЕ КОД              │
├──────────────────────────┤
│                          │
│ Код: [____________________]
│ Новый пароль: [__________]
│ Повторить: [_____________]
│                          │
│ [Восстановить пароль]    │
│                          │
└──────────────────────────┘
```

**ЧТО ПРОИСХОДИТ?**

```
Пользователь забыл пароль
  ↓
На странице входа кликает "Забыли пароль?"
  ↓
Вводит email
  ↓
Кликает "Отправить код"
  ↓
Frontend вызывает:
POST /api/auth/forgot-password
{ email }
  ↓
Better-auth:
- Проверяет есть ли такой email
- Генерирует токен восстановления
- Отправляет email с кодом
- Сохраняет токен в таблице passwordResets
  ↓
Пользователь получает email:
"Ваш код восстановления: ABC123XYZ"
  ↓
Вводит код и новый пароль
  ↓
Frontend вызывает:
POST /api/auth/reset-password
{ token, newPassword }
  ↓
Better-auth:
- Проверяет токен (не истёк ли)
- Хеширует новый пароль
- Обновляет пароль в БД
- Удаляет токен восстановления
  ↓
Пользователь может войти с новым паролем
```

### КОМПОНЕНТ 4: ВХОД ЧЕРЕЗ GOOGLE

**ЧТО ЭТО?** Кнопки для входа через социальные сети

**ЧТО ДОЛЖНО БЫТЬ?**

```
На странице входа:
[🔵 Войти через Google]
```

**ЧТО ПРОИСХОДИТ?**

```
Пользователь кликает "Войти через Google"
  ↓
Better-auth перенаправляет на:
https://accounts.google.com/o/oauth2/auth?...
  ↓
Пользователь вводит свой Google пароль
  ↓
Google возвращает токен
  ↓
Better-auth обрабатывает:
- Проверяет есть ли такой email в users
- Если нет → создаёт нового пользователя
- Создаёт аккаунт в таблице accounts
  (помечает как provider: "google")
- Создаёт сессию
  ↓
Frontend получает токен
  ↓
Пользователь вошел!
```

---

## 🛡️ ЭТАП 5: ЗАЩИТА МАРШРУТОВ (СТРАНИЦ) (1-2 часа)

### ЧТО ЭТО?

Убедиться что только авторизованные пользователи видят защищённые страницы

### ЧТО ДЕЛАТЬ?

**На Frontend:**

Создать компонент ProtectedRoute, который:

1. Проверяет есть ли токен в localStorage

2. Вызывает GET /api/auth/session (передаёт токен)

3. Если Backend вернул пользователя -> показать страницу

4. Если нет -> перенаправить на страницу входа

**На Backend:**

Для каждого защищённого endpoint проверить:

```
1. Есть ли токен в заголовке Authorization
2. Валиден ли токен
3. Не истекла ли сессия
4. Только если всё ок → обработать запрос
```

### ЧТО ДОЛЖНО ПОЛУЧИТЬСЯ?

```
Пользователь пытается перейти на /events
  ↓
Frontend проверяет: есть ли токен?
  ├─ Нет → перенаправить на /login
  └─ Да → отправить GET /api/auth/session
       ↓
Backend проверяет:
  ├─ Токен валиден?
  ├─ Сессия не истекла?
  └─ Если ДА → возвращает пользователя
             → Frontend показывает /events
       └─ Если НЕТ → возвращает 401 Unauthorized
                  → Frontend перенаправляет на /login
```

---

## 📱 ЭТАП 6: СОХРАНЕНИЕ ТОКЕНА И СЕССИИ (1-2 часа)

### ЧТО НУЖНО СДЕЛАТЬ?

**Шаг 1:** При успешной регистрации/входе

```
Backend возвращает токен: "eyJhbGciOiJIUzI1NiIs..."
Frontend сохраняет его в localStorage:
localStorage.setItem("authToken", token)
```

**Шаг 2:** При каждом запросе к защищённому endpoint

```
Frontend берёт токен из localStorage
Добавляет его в заголовок:
Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
Отправляет запрос
```

**Шаг 3:** При загрузке приложения

```
Frontend проверяет: есть ли токен в localStorage?
  Если ДА → вызывает GET /api/auth/session
       → получает данные пользователя
       → показывает приложение
  Если НЕТ → показывает страницу входа
```

**Шаг 4:** При выходе (логаут)

```
Пользователь кликает "Выход"
Frontend вызывает: POST /api/auth/sign-out
Backend удаляет сессию из таблицы sessions
Frontend удаляет токен из localStorage
Показывает страницу входа
```

### ЧТО ДОЛЖНО ПОЛУЧИТЬСЯ?

```
localStorage:
{
  "authToken": "eyJhbGciOiJIUzI1NiIs...",
  "user": {
    "id": "user_123",
    "email": "petrov@mail.com",
    "name": "Петр"
  }
}

Когда пользователь закрывает приложение:
→ Токен остаётся в localStorage
→ При следующем открытии пользователь уже вошел!

Когда сессия истекает (24 часа):
→ Backend возвращает 401
→ Frontend очищает localStorage
→ Показывает страницу входа
```

---

## 🎯 ЭТАП 7: ИНТЕГРАЦИЯ С КОМПОНЕНТАМИ ПРИЛОЖЕНИЯ (1-2 часа)

### ЧТО ДЕЛАТЬ?

**Шаг 1:** В главном меню приложения показать:

```
Если пользователь НЕ авторизован:
  [Вход]  [Регистрация]

Если пользователь авторизован:
  [👤 Петров] ↓
    ├── Профиль
    ├── История ставок
    ├── Баланс
    └── [Выход]
```

**Шаг 2:** При клике на "Выход":

```
Frontend вызывает POST /api/auth/sign-out
Backend завершает сессию
Frontend перенаправляет на страницу входа
```

**Шаг 3:** На странице профиля показать:

```
┌────────────────────────────┐
│ Мой профиль                │
├────────────────────────────┤
│                            │
│ Имя: Петров [✏️ Изменить]  │
│ Email: petrov@mail.com     │
│ Дата регистрации: 15.12.25 │
│                            │
│ [✏️ Изменить пароль]       │
│ [🚪 Выход]                 │
│                            │
└────────────────────────────┘
```

### ЧТО ДОЛЖНО ПОЛУЧИТЬСЯ?

```
Приложение знает кто вошел:
✅ Может показать имя пользователя
✅ Может показать его историю ставок
✅ Может показать его баланс
✅ Только ЭТОГО пользователя ставки видит

Безопасность:
✅ Без токена → не видит защищённые страницы
✅ С чужим токеном → получит 401 ошибку
```

---

## 💾 ТАБЛИЦЫ БД: ПОДРОБНО

### ТАБЛИЦА: users

```
id            VARCHAR(20)  PRIMARY KEY
email         VARCHAR(100) UNIQUE NOT NULL
emailVerified BOOLEAN      DEFAULT FALSE
name          VARCHAR(100)
image         VARCHAR(500)
createdAt     TIMESTAMP    DEFAULT NOW()
updatedAt     TIMESTAMP    DEFAULT NOW()

Пример строки:
id           | email          | emailVerified | name   | createdAt
user_123     | petrov@mail.com| true          | Петров | 2025-12-15 10:30

Зачем:
- user_123 — уникальный ID для связи с другими таблицами
- emailVerified — проверил ли пользователь почту
- name — отображается в приложении
```

### ТАБЛИЦА: sessions

```
id         VARCHAR(20)  PRIMARY KEY
userId     VARCHAR(20)  FOREIGN KEY → users.id
token      VARCHAR(500) UNIQUE
expiresAt  TIMESTAMP    NOT NULL
createdAt  TIMESTAMP    DEFAULT NOW()

Пример строки:
id          | userId    | token                    | expiresAt
session_456 | user_123  | eyJhbGciOiJIUzI1NiIs... | 2025-12-29 10:30

Зачем:
- Каждый раз когда пользователь входит → создаётся новая сессия
- Token — передаётся на Frontend (сохраняется в localStorage)
- expiresAt — время истечения сессии (обычно 24 часа)
- Когда сессия истекает → пользователь должен войти снова
```

### ТАБЛИЦА: accounts

```
id                  VARCHAR(20)  PRIMARY KEY
userId              VARCHAR(20)  FOREIGN KEY → users.id
provider            VARCHAR(50)  (email, google, github)
providerAccountId   VARCHAR(100)
accessToken         VARCHAR(500)
refreshToken        VARCHAR(500)
expiresAt           TIMESTAMP

Пример строки 1 (email/пароль):
id        | userId    | provider | providerAccountId | accessToken | ...
acct_111  | user_123  | email    | (null)           | (null)      | ...

Пример строки 2 (Google):
id        | userId    | provider | providerAccountId | accessToken        | ...
acct_222  | user_123  | google   | 110234567890123456789 | ya29.a0AfH6SMB... | ...

Зачем:
- Если пользователь входит через email → provider = "email"
- Если через Google → provider = "google", providerAccountId = ID от Google
- accessToken — для взаимодействия с Google API
- Один пользователь может иметь несколько accounts (email + Google)
```

### ТАБЛИЦА: verifications

```
id         VARCHAR(20)  PRIMARY KEY
identifier VARCHAR(100) (email или телефон)
value      VARCHAR(500) (код для верификации)
expiresAt  TIMESTAMP    NOT NULL
createdAt  TIMESTAMP    DEFAULT NOW()

Пример строки:
id       | identifier      | value    | expiresAt
verif_1  | petrov@mail.com | ABC123XY | 2025-12-15 11:35

Зачем:
- Когда пользователь регистрируется → создаётся верификационный код
- Отправляется на email
- Пользователь вводит код → удаляется из verifications
- emailVerified в таблице users становится true
```

### ТАБЛИЦА: passwordResets

```
id        VARCHAR(20)  PRIMARY KEY
userId    VARCHAR(20)  FOREIGN KEY → users.id
email     VARCHAR(100)
token     VARCHAR(500) (код для восстановления)
expiresAt TIMESTAMP    NOT NULL
createdAt TIMESTAMP    DEFAULT NOW()

Пример строки:
id        | userId    | email           | token         | expiresAt
reset_1   | user_123  | petrov@mail.com | XYZ789ABC123  | 2025-12-15 11:35

Зачем:
- Когда пользователь кликает "Забыли пароль"
- Создаётся токен восстановления
- Отправляется на email
- Пользователь вводит код и новый пароль
- Пароль обновляется в users
- Строка удаляется из passwordResets
```

---

## 📋 ИТОГОВЫЙ ЧЕК-ЛИ ST

### BACKEND:

* [ ] Better-auth установлен (`npm install better-auth`)

* [ ] PostgreSQL адаптер установлен (`npm install @better-auth/pg`)

* [ ] Better-auth инициализирован в `app/main.py`

* [ ] Все 5 таблиц созданы в PostgreSQL:

   * [ ] users

   * [ ] sessions

   * [ ] accounts

   * [ ] verifications

   * [ ] passwordResets

* [ ] Все 9 endpoints работают:

   * [ ] POST /api/auth/sign-up

   * [ ] POST /api/auth/sign-in

   * [ ] POST /api/auth/sign-out

   * [ ] GET /api/auth/session

   * [ ] PUT /api/auth/user

   * [ ] POST /api/auth/change-password

   * [ ] POST /api/auth/forgot-password

   * [ ] POST /api/auth/reset-password

   * [ ] POST /api/auth/verify-email

### FRONTEND:

* [ ] React компоненты создены:

   * [ ] SignUp.tsx (регистрация)

   * [ ] SignIn.tsx (вход)

   * [ ] ForgotPassword.tsx (восстановление)

   * [ ] AuthService.ts (вызовы API)

* [ ] Токен сохраняется в localStorage

* [ ] ProtectedRoute компонент работает

* [ ] При выходе токен удаляется

* [ ] Интеграция с меню приложения

* [ ] Страница профиля готова

### БЕЗОПАСНОСТЬ:

* [ ] Пароли хешируются (SHA-256)

* [ ] Токены имеют срок действия

* [ ] Защищённые страницы недоступны без токена

* [ ] CORS включен только для своего домена

---

## 🚀 ИТОГОВЫЙ РЕЗЮМЕ

### ЧТО Better-auth ПРЕДОСТАВЛЯЕТ (ГОТОВОЕ):

1. **Таблицы в БД** -- создаёт все 5 таблиц автоматически

2. **API endpoints** -- все 9 endpoints из коробки

3. **Хеширование паролей** -- безопасное хранение

4. **Управление сессиями** -- создание/удаление/проверка

5. **Верификация email** -- отправка кодов

6. **Восстановление пароля** -- полный процесс

7. **OAuth** -- вход через Google

### ЧТО ДОЛЖЕН СДЕЛАТЬ:

1. **Установить better-auth** (1 команда)

2. **Инициализировать** в Backend (5 строк кода)

3. **Создать UI компоненты** на Frontend:

   -  Форма регистрации

   -  Форма входа

   -  Форма восстановления пароля

   -  Компонент профиля

4. **Интегрировать с приложением** -- добавить ссылки и меню

5. **Настроить защиту** -- только авторизованные видят события

### РЕЗУЛЬТАТ:

```
✅ Полная система авторизации
✅ Безопасное хранение паролей
✅ Управление сессиями
✅ Красивый интерфейс входа
✅ Восстановление пароля
✅ Интеграция с социальными сетями
✅ Готово к production!
```